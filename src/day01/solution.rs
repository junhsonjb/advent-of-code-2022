fn solution(input_file) -> usize {

    // TODO:
    // - read file and parse sum of each calorie group
    // - enter sums into max heap (or keep running max?)
    // - when parsing is done, return largest calorie sum

}

fn main() {
    let inputFile = "input.txt";
    println!("The answer is {}", solution(inputFile))
}
